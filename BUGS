# bug[0]	"config()" doesn't allow the user to overwrite
#			single setting entries.  Attempting to 
#			overwrite one affixes the default to unedited
#			settings. -- FIXED --
#
# bug[1]	"update()" doesn't calculate estimated time
# 			slept per scope correctly anymore. -- FIXED --
#
# bug[2]	Errors in changing unit, corrupting
# 			current bank. -- FIXED --
#
# General concerns:
#
#	<none>
#
